# npm configuration for hosting environments
# Note: npm doesn't have a built-in way to ignore engines via .npmrc
# Use --ignore-engines flag with npm install if needed
# legacy-peer-deps=false

# This file is kept for future npm configurations

